// features.h
#ifndef FEATURES_H
#define FEATURES_H

class Features {
public:
    Features();
    ~Features();

    void init();
    void update();

private:
    // List of features (e.g. AimAssist, Reach, etc.)
    //...
};

#endif // FEATURES_H
